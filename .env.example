# pi-weixin-bridge 环境变量示例
# 复制为 .env 或直接设置系统环境变量；以下为可选配置，均有默认值。

# 状态目录（账号凭据 account.json、媒体缓存等）。默认 ~/.pi-weixin-bridge
# PI_WEIXIN_STATE_DIR=C:\Users\<you>\.pi-weixin-bridge

# pi 会话的工作目录（Agent 在此目录读写文件、接收的媒体落盘）。默认 D:\pi_weixin_project
# PI_WEIXIN_WORKSPACE=D:\pi_weixin_project
